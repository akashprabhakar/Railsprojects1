%p#notice= notice

.bg-headers 
	%h1= @post.title
%fieldset
	= image_tag @post.image.url
	%p= raw (coderay(RedCloth.new(@post.body).to_html))
		

-content_for :aside do
	= link_to 'Edit Post', edit_post_path(@post)
	= link_to 'Back', posts_path
